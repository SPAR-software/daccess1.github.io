apiVersion: v1
kind: Service
metadata:
  name: umperium-dev-game-frontend
spec:
  selector:
    app: umperium-dev-game-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
