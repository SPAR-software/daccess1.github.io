apiVersion: apps/v1
kind: Deployment
metadata:
  name: umperium-frontend
  labels:
    app: umperium-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: umperium-frontend
  template:
    metadata:
      name: umperium-frontend
      labels:
        app: umperium-frontend
    spec:
      containers:
        - name: umperium-frontend
          image: cr.yandex/crprep7mlarphamjggv1/frontend/umperium-frontend:1.2.0
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 100m
              memory: 50Mi
            requests:
              cpu: 50m
              memory: 25Mi
          ports:
            - containerPort: 80
              protocol: TCP
      restartPolicy: Always
      
